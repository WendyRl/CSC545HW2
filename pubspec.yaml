name: image_store
description: "A new Flutter project."
publish_to: 'none'  # Prevent accidental publishing

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.0.8
  shared_preferences: ^2.0.13
  image_picker: ^0.8.5+3
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

# Add any additional settings such as assets or fonts here, following the Flutter documentation.
